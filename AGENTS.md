# Veyra 项目开发约束

## 开发阶段兼容性政策

Veyra 当前处于开发阶段。实现新设计时，以新设计的一致性和简洁性为优先，不兼容已经废弃的旧设计。

- 允许对内部 API、领域模型、事件 Schema、持久化格式、文件布局和恢复协议进行破坏性改造。
- 不保留旧事件、旧数据文件或旧持久化结构。
- 不实现旧数据的兼容读取、双写、回退解析、在线迁移或离线迁移。
- 不得仅为兼容旧设计而保留已废弃的字段、事件类型、状态、适配器或分支逻辑。
- 新设计落地时，应同步删除被替代的旧实现及其测试，测试数据统一按新格式重新生成。
- 如果本政策与历史设计文档中的兼容或迁移方案冲突，以本政策为准，并同步修正文档。

除非用户在具体任务中明确要求兼容某项外部协议，否则不得自行增加兼容层。
